import { ImageFallback } from './image-fallback'

describe('ImageFallback', () => {
  it('should create an instance', () => {
    const directive = new ImageFallback()
    expect(directive).toBeTruthy()
  })
})
